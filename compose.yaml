services:
  sayanything:
    build: ./server
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - chat-data:/data
      - ./dist:/downloads:ro
    environment:
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
    restart: unless-stopped
volumes:
  chat-data:
